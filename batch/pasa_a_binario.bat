@echo off

rem Establecemos la página de códigos para que caracteres especiales 
rem sean imprimibles
chcp 65001

rem Imprime por pantalla el mensaje 
rem Introduzca el número a traducir a binario
rem y almacénalo en una variable.

rem Imoplementa el método de las divisiones sucesivas para ir generando 
rem una cadena de caracteres con ceros y unos que almacenarás en la cvariable
rem binario

rem Es probable que tengáis que usar if, etiquetas y la orden goto.

rem imprime por pantalla el mensaje
rem El número decimal X es Y en binario.
rem donde X es el nñumero decimal introducido por el usuario e
rem Y la cadena de caracteres equivalente en binario a X.
